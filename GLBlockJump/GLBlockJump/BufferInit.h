#ifndef BUFFER_INIT_H
#define BUFFER_INIT_H

#include "stdafx.h"

void InitBuffer();

extern GLuint vaoCube, vboCube[3], eboCube;
extern GLuint vaofloor, vbofloor[3], ebofloor;
extern GLuint vaoSLB, vboSLB[3];
extern GLuint vaoELB, vboELB[3];

#endif